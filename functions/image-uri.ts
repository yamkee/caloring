const friendImageUri = (level: number) => {
    switch (level) {
        case 1:
            return '../../assets/test.png'
        default:
            return '../../assets/test.png'
    }
}

export { friendImageUri }
